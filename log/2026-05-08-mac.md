# 2026-05-08 — Mac session log

- Set up the sync protocol (`CLAUDE.md`) so Mac and Windows sessions
  stay on the same level of information.
- Created `SHARED_KNOWLEDGE.md` as the running shared brain.
- Asked Windows to push its current Pine Script source into the repo.
- Confirmed (via `ls /` from the sandbox) that Browser Claude runs on
  Linux with no Mac filesystem and no OneDrive access. OneDrive sync
  alone cannot reach Browser Claude.
- Rewrote `MESSAGE_TO_WINDOWS.md` with the diagnosis and a request:
  either dual-write memory to git, or move memory entirely to git.
  Recommended Option B (git as single source of truth).
